{% extends "base.html.j2" %}
{% block title %}Product Details{% endblock %}
{% block h2 %}<h2>Product Details</h2>{% endblock %}
{% block content %}
<p>Sorry, this application is under construction.</p>
<p>(products/show.html.j2)</p>
<p>{{ product.__dict__ }}<br>
    <a href="{{ url_for('manufacturers.show_manufacturer', id=product.manufacturer.id) }}">details of {{ product.manufacturer.short_brand_name }}</a><br>
    <a href="{{ url_for('.edit_product', id=product.id) }}">edit</a>
    <form method="POST" action="{{ url_for('.delete_product', id=product.id) }}">
        <button type="submit">delete</button>
    </form>
</p>
<p>
    <a href="{{ url_for('.products') }}">back to all products</a>
</p>
{% endblock %}